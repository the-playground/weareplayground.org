import React from 'react';
import styled from 'styled-components';

import { Heading, BodyText, Section, Container } from '@nerve/core/components';
import { breakpoints, grid, spacing } from '@nerve/core/tokens';

const StyledTaxExemptSection = styled(Section)``;

export const TaxExemptSection = () => {
    <StyledTaxExemptSection />;
};
