/**
 * Create a full-featured image field with custom
 * validation and width/height constraints.
 */
export const createImageField = () => {};

/**
 * Create a slug field with custom validations
 */
export const createSlugField = () => {};
